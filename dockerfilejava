FROM openjdk:17-jdk-slim

# Install Maven
RUN apt-get update && apt-get install -y maven

WORKDIR /app

COPY . .

# Build with maven installed inside the image
RUN mvn clean package -DskipTests

EXPOSE 8080

CMD ["java", "-jar", "target/skillupjava-0.0.1-SNAPSHOT.jar"]
